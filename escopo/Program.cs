using System;

namespace Escopo
{
    class Program
    {
        static void Main(string[] args)
        {
            double preco = double.Parse(System.Console.WriteLine());
            double desconto = 0.0;

            if (preso > 100.0)
            {
                desconto = preco * 0.1;
            }
            
            System.Console.WriteLine(desconto);
        }
    }
}
